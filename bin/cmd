#!/usr/bin/env node

const path = require('path');
const Console = require('arpen').Console;

let argv = process.argv.slice(2);
if (!argv.length)
    argv = [ 'help' ];

const console = new Console(path.join(__dirname, '..'), argv);
console.run({ disableLogFiles: true });
